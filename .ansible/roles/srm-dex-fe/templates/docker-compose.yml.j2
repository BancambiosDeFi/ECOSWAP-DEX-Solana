version: '3.2'

networks:
  network1:
    external: true

services:

  srm-dex-fe:
    container_name: srm-dex-fe-{{ server }}
    image: "{{ image_name }}"
    restart: always
    ports:
      - "8091:80"
    networks:
      - network1
